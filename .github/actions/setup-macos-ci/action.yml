name: Setup macOS CI
description: >-
  Installs Ruby + bundler dependencies (root and ExampleHTTP) and runs
  `pod install` for the ExampleHTTP workspace, using the runner image's
  default Xcode.

inputs:
  ruby-version:
    description: >-
      Ruby version to install. Must satisfy the committed Gemfile.lock files,
      which pin activesupport 7.2.2.1 (requires Ruby >= 3.1.0). An older Ruby
      makes Bundler silently downgrade activesupport and rewrite both lockfiles.
    required: false
    default: "3.1.4"
  install-pods:
    description: Whether to run `pod install` for the ExampleHTTP workspace.
    required: false
    default: "true"

runs:
  using: composite
  steps:
    - name: Install Ruby ${{ inputs.ruby-version }}
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: ${{ inputs.ruby-version }}
        # Take Bundler from the lockfiles' `BUNDLED WITH` (this is the default,
        # but it's load-bearing here): a different Bundler rewrites that section
        # and trips the dirty-tree check.
        bundler: Gemfile.lock
        # Gems are cached per-Gemfile.lock below; there are two bundles in this
        # repo (root and ExampleHTTP) and bundler-cache only handles one.
        bundler-cache: false

    - name: Restore root Ruby gem cache
      uses: actions/cache@v4
      with:
        path: .vendor/bundle
        key: v1-ruby-${{ inputs.ruby-version }}-gems-${{ hashFiles('Gemfile.lock') }}
        restore-keys: |
          v1-ruby-${{ inputs.ruby-version }}-gems-

    - name: Install root bundler dependencies
      shell: bash
      run: |
        set -euo pipefail
        bundle config set --local path '.vendor/bundle'
        bundle config set --local clean 'true'
        bundle check || bundle install --jobs 4 --retry 3

    - name: Restore ExampleHTTP Ruby gem cache
      uses: actions/cache@v4
      with:
        path: ExampleHTTP/.vendor/bundle
        key: v1-example-http-ruby-${{ inputs.ruby-version }}-gems-${{ hashFiles('ExampleHTTP/Gemfile.lock') }}
        restore-keys: |
          v1-example-http-ruby-${{ inputs.ruby-version }}-gems-

    - name: Install ExampleHTTP bundler dependencies
      shell: bash
      working-directory: ExampleHTTP
      run: |
        set -euo pipefail
        bundle config set --local path '.vendor/bundle'
        bundle config set --local clean 'true'
        bundle check || bundle install --jobs 4 --retry 3

    # `Pods/CocoaPodsKeys` holds the generated test credentials, so no cache may
    # carry it: a cache saved on master is restorable by a fork pull request.

    # cocoapods-keys resolves each key from ENV before falling back to the
    # keychain, so the caller only needs to export the key names as job env.
    - name: Install pods for ExampleHTTP
      if: inputs.install-pods == 'true'
      shell: bash
      working-directory: ExampleHTTP
      run: bundle exec pod install --deployment

    - name: Print tool versions
      shell: bash
      run: |
        set -x
        sw_vers
        xcodebuild -version
        ruby --version
        bundle env
        bundle exec pod env
